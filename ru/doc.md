# docubridge 📚

> MCP сервер для мгновенного доступа к твоим локальным документациям

**docubridge** — это [Model Context Protocol (MCP)](https://modelcontextprotocol.io) сервер, который даёт AI ассистентам (Qwen, Claude и др.) прямой доступ к документациям, которые ты сам указал. Вместо того чтобы модель угадывала или галлюцинировала детали API — она читает реальную документацию.

---

## Зачем это нужно?

Когда работаешь с LLM в терминале, модель не знает:
- какую версию FastAPI ты используешь
- что написано в документации твоего внутреннего проекта
- любую документацию, которую ты добавил сам

**docubridge** решает это, предоставляя твои локальные Markdown документации как MCP инструменты — модель может их листать, читать и искать по ним по запросу.

---

## Возможности

| Инструмент | Описание |
|---|---|
| `list_libraries` | Показать все доступные библиотеки документаций |
| `list_files` | Список всех файлов внутри конкретной библиотеки |
| `get_file` | Прочитать содержимое конкретного файла |
| `search_docs` | Поиск по ключевому слову по всем докам или внутри библиотеки |

---

## Структура проекта

```
mcp-docs-server/
├── pyproject.toml
├── .env
├── README.md
├── README.ru.md
└── src/
    └── docs_server/
        ├── __init__.py
        ├── main.py
        ├── server.py
        ├── config.py
        └── reader.py
```

---

## Установка

```bash
git clone https://github.com/yourname/docubridge.git
cd docubridge
pip install -e .
```

---

## Добавление документации

Клонируй любую документацию у которой есть Markdown исходники. Например, FastAPI:

```bash
git clone --depth=1 --filter=blob:none --sparse https://github.com/fastapi/fastapi.git
cd fastapi
git sparse-checkout set docs/en/docs
```

Затем перемести папку в директорию `docs/`:

```
docs/
└── fastapi/
    ├── index.md
    ├── tutorial/
    └── advanced/
```

Можешь добавлять сколько угодно библиотек — просто кидай папку в `docs/`.

---

## Конфигурация

Создай файл `.env` в корне проекта:

```env
DOCS_DIR=./docs
```

---

## Подключение к Qwen CLI

Добавь в свой `.qwen/settings.json`:

```json
{
  "mcpServers": {
    "docubridge": {
      "command": "docs-server",
      "timeout": 15000
    }
  }
}
```

Затем проверь подключение:

```bash
qwen mcp list
```

В списке должен появиться `docubridge`.

---

## Примеры использования

После подключения можешь спрашивать у своего AI ассистента:

- *«Какие библиотеки у тебя есть?»*
- *«Покажи все файлы в документации fastapi»*
- *«Найди всё про dependency injection в fastapi»*
- *«Прочитай содержимое tutorial/path-params.md»*

Модель вызовет нужный инструмент и ответит на основе реальной документации.

---

## Требования

- Python 3.11+
- `mcp[cli]` >= 1.0.0
- `pydantic-settings`

---

## Лицензия

MIT